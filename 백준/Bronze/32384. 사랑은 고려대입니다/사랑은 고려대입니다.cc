#include <iostream>
#include <string>
#include <cstring>
#include <vector>
#include <list>
#include <map>
#include <array>
#include <algorithm>
#include <memory>
#pragma warning(disable:4996) // scanf

int main() {

	int N;
	scanf("%d", &N);

	for (int i = 0; i < N; i++)
	{
		printf("LoveisKoreaUniversity ");
	}

	return 0;
}
