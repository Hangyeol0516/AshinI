#include <iostream>

int main() {
    
    int N, K;
    std::cin >> N;
    std::cin >> K;

    for (int i = 0; i < N; i++)
    {
        int A, B;
        std::cin >> A;
        std::cin >> B;
    }

    std::cout << "비와이" << std::endl;
}